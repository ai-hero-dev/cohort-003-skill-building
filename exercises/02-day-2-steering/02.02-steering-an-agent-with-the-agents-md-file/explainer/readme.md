# Steering An Agent With The Agents.md File

Create a situation where an agent makes poor decisions, then steer it with an AGENTS.md file to enforce better patterns.

Build a bookmarks feature:
- Let students bookmark lessons
- Use AGENTS.md to guide against positional parameters
- Verify the agent follows the guidance
